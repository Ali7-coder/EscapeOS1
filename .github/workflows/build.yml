name: Build EscapeOSPro

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5   # تم الترقية من v4 إلى v5 لحل تحذير Node.js 20[reference:5]

      - name: Install missing build tools
        run: |
          brew install gzip

      - name: Setup Theos
        uses: Randomblock1/theos-action@v1
        with:
          theos-dir: '${{ github.workspace }}/theos'
          theos-src: 'https://github.com/theos/theos'
          theos-sdks: 'https://github.com/theos/sdks'
        env:
          HOMEBREW_NO_REQUIRE_TAP_TRUST: 1

      - name: Build with Theos
        run: |
          export THEOS=${{ github.workspace }}/theos
          make package FINALPACKAGE=1
        env:
          # تجاوز تحذير Node.js 20 (حل مؤقت)
          ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

      - name: Upload IPA as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: EscapeOSPro-IPA
          path: ./*.ipa

      - name: Create Release and Upload IPA
        run: |
          TAG_NAME="build-${{ github.run_number }}"
          gh release create "$TAG_NAME" ./*.ipa \
            --title "EscapeOSPro ${{ github.run_number }}" \
            --prerelease \
            --generate-notes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
